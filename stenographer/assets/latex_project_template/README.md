# LaTeX project template (used by $stenographer)

This folder is a template. Copy it next to `paper.md` as `<basename>_latex/`.

Build output is produced into `<basename>_latex/build/` and then copied to `../<basename>.pdf`.
